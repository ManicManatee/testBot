opencv-python>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0
PyYAML>=6.0
pytesseract>=0.3.10
customtkinter>=5.2.0

# Build tools (PyInstaller etc.) moved to requirements-build.txt —
# end users running the bot don't need them.
